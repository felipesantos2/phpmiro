services:
  php:
    image: php84-alpine
    container_name: phptests
    build:
      context: .
    ports:
      - '8000:8000'
    volumes:
      - '.:/app'
    command: ["php", "-S", "0.0.0.0:8000", "-t", "public/"]
